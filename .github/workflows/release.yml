name: Build and Release

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

permissions:
  contents: write

jobs:
  build:
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      startsWith(github.event.workflow_run.head_branch, 'v')

    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            artifact: homunc-linux-x86_64
            binary: homunc
            ext: ""
          - target: aarch64-unknown-linux-gnu
            artifact: homunc-linux-aarch64
            binary: homunc
            ext: ""
          - target: x86_64-pc-windows-gnu
            artifact: homunc-windows-x86_64
            binary: homunc.exe
            ext: ".exe"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_branch }}

      - name: Build with Docker
        run: |
          docker buildx build \
            --build-arg TARGET=${{ matrix.target }} \
            --cache-from type=gha \
            --cache-to type=gha,mode=max \
            --output type=local,dest=./out .

      - name: Rename binary
        run: mv out/${{ matrix.binary }} "${{ matrix.artifact }}-${{ github.event.workflow_run.head_branch }}${{ matrix.ext }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}-${{ github.event.workflow_run.head_branch }}${{ matrix.ext }}
          path: ${{ matrix.artifact }}-${{ github.event.workflow_run.head_branch }}${{ matrix.ext }}

  build-wasm:
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      startsWith(github.event.workflow_run.head_branch, 'v')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_branch }}

      - name: Build WASM with Docker
        run: |
          docker buildx build \
            -f Dockerfile.wasm \
            --cache-from type=gha \
            --cache-to type=gha,mode=max \
            --output type=local,dest=./out .

      - name: Package WASM
        run: tar czf "homunc-wasm-${{ github.event.workflow_run.head_branch }}.tar.gz" -C out .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: homunc-wasm-${{ github.event.workflow_run.head_branch }}.tar.gz
          path: homunc-wasm-${{ github.event.workflow_run.head_branch }}.tar.gz

  release:
    runs-on: ubuntu-latest
    needs: [build, build-wasm]
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create GitHub Release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ github.event.workflow_run.head_branch }}
          token: ${{ secrets.GITHUB_TOKEN }}
          generateReleaseNotes: true
          artifacts: artifacts/*
          allowUpdates: true
